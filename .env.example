# Where the SQLite database file is stored. Defaults to ./data/project-tracker.db
# DATABASE_PATH=./data/project-tracker.db

# Optional: protect the whole app (dashboard + API) with HTTP Basic Auth.
# Leave unset for local development. Set both when deploying publicly.
# APP_USERNAME=admin
# APP_PASSWORD=change-me
