# project-tracker 運用ルール

このリポジトリは、複数プロジェクトを横断した個人タスクを管理する静的サイト（GitHub Pagesで公開・read-only）。
タスクデータの実体は `data/tasks.json`。サイトからは編集できないため、更新はすべてClaude Code経由で行う。

## タスク更新のワークフロー

ユーザーが会話の中で以下のようなことを伝えてきたら、確認を挟まずに次を行ってよい:

- 「〇〇のタスク追加して」→ `data/tasks.json` に新しいタスクを追加（`id`は`Date.now()`ベースなど一意な文字列、`createdAt`/`updatedAt`は現在時刻のISO文字列、`history`に作成エントリを1件）
- 「〇〇終わった / 完了した」→ 該当タスクの `status` を `"完了"` に、`updatedAt` を現在時刻に更新し、`history` の先頭に変更内容を1件追加
- 「〇〇の状況変わった / 進行中にして」など→ 該当フィールドを更新し、同様に `history` に記録
- 更新後は **その場で `git add data/tasks.json && git commit && git push` まで実行してよい**（このファイルへの変更に限り、pushの都度ユーザーに確認を取らなくてよい）

該当タスクが複数候補あって特定できない場合や、タスクの削除など取り消しにくい操作をする場合は、実行前にユーザーに確認する。

## スコープ外の操作

上記の自動push許可は `data/tasks.json` の更新コミットのみに適用される。以下は通常どおり実行前にユーザーへの確認が必要:

- `index.html` / `css/` / `js/` などサイトの実装変更
- force push、履歴の書き換え、ブランチ削除などの破壊的操作
- リポジトリ設定（公開範囲、Pages設定など）の変更

## データ形式

README.md の「データ形式」セクションを参照。
